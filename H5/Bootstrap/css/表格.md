# 表格

> bootstrap对表格做了很大的优化，我们直接拿来就能用

## 最基本的表格元素标签

| 标签                                | 说明           |
| --------------------------------- | ------------ |
| `.container` & `.container-fluid` | bootstrap的容器 |
| `<table>`                         | 起始标签         |
| `<thead>`                         | 头部起始标签       |
| `<tbody>`                         | 内容起始标签       |
| `<tr>`                            | 行标签          |
| `<th>`                            | 列标签中的标题标签    |
| `<td>`                            | 列标签中的内容标签    |

> `bootstrap` 下最简单的表格案例：

```html
<section class="container">
    <table class="table">
      <thead>
        <tr>
          <th>#</th>
          <th>First</th>
          <th>Last</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th>1</th>
          <td>Mark</td>
          <td>Otto</td>
        </tr>
        <tr>
          <th>2</th>
          <td>Jacob</td>
          <td>Thornton</td>
        </tr>
      </tbody>
    </table>
</section>
```

## `<td> & <th>` 可选属性

> 下面展示几个 `td & th` 标签重要的可选属性：

| 属性        | 说明                  |
| --------- | ------------------- |
| `align`   | 规定单元格内容的水平对齐方式。     |
| `valign`  | 规定单元格内容的垂直排列方式。     |
| `scope`   | 定义将表头数据与单元数据相关联的方法。 |
| `rowspan` | 规定单元格可横跨的行数。        |
| `colspan` | 设置单元格可横跨的列数。        |

> 案例：

## 表格相邻 `<tbody>`

> `<table>` 下如果有两个相邻的 `<tbody>` 子标签，会有一个2px的灰色便不可分割

```css
/* bootstrap 源码*/
.table tbody + tbody {
  border-top: 2px solid #dee2e6;
}
```

## 表格行高

> `bootstrap` 用 `.table-sm` 来降低表格行高

| `.table-sm`类所在位置 | 表格效果                 |
| ---------------- | -------------------- |
| `<table>`        | `整个表格`内的行高都会减低       |
| `<thead>`        | 表格中 `头部起始标签` 内的行高会降低 |
| `<tbody>`        | 表格中 `内容起始标签` 内的行高会降低 |
| `<tr>`           | 表格中指定 `<tr>` 下的行高会降低 |

> bootstrap源码

```css
.table-sm th,
.table-sm td {
  padding: 0.3rem;
}
```

> 案例1：整个表格，行高降低

```html
<section class="container">
    <table class="table table-sm">
      <thead>
        <tr>
          <th>#</th>
          <th>First</th>
          <th>Last</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th>1</th>
          <td>Mark</td>
          <td>Otto</td>
        </tr>
      </tbody>
    </table>
</section>
```

> 案例2：表格`内容起始标签`的第一行，行高降低

```html
<section class="container">
    <table class="table">
      <thead>
        <tr>
          <th>#</th>
          <th>First</th>
          <th>Last</th>
        </tr>
      </thead>
      <tbody>
        <tr class="table-sm">
          <th>1</th>
          <td>Mark</td>
          <td>Otto</td>
        </tr>
        <tr>
          <th>2</th>
          <td>Jacob</td>
          <td>Thornton</td>
        </tr>
      </tbody>
    </table>
</section>
```

## 表格边框

> `bootstrap` 处理边框的`css类`有两个：

| 代码                  | 作用   |
| ------------------- | ---- |
| `.table-bordered`   | 带上边框 |
| `.table-borderless` | 取消边框 |

> 案例1：整个表格带上边框

```html
<section class="container">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>#</th>
          <th>First</th>
          <th>Last</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th>1</th>
          <td>Mark</td>
          <td>Otto</td>
        </tr>
      </tbody>
    </table>
</section>
```

> 案例2：整个表格带上边框，但是`<tbody>`内的第一行不带标签

```html
<section class="container">
    <table class="table table-bordered">
      <thead class="thead-light">
        <tr>
          <th>#</th>
          <th>First</th>
          <th>Last</th>
        </tr>
      </thead>
      <tbody>
        <tr class="table-borderless">
          <th>1</th>
          <td>Mark</td>
          <td>Otto</td>
        </tr>
        <tr>
          <th>2</th>
          <td>Jacob</td>
          <td>Thornton</td>
        </tr>
      </tbody>
    </table>
</section>
```

> PS： `.table-bordered & .table-borderless` 也可以运用到 `<th> & <td>` 这种最小单元，这里就不举例了

## 表格背景

> 表格背景分为以下几类，并且代码不尽相同

### 仅针对 `<th>`

> `.thead-dark & .thead-light` 仅针对 `<th>` 标签，必须定义在 `<thead> & <tbody>`

| css类           | 效果           | 定义样式的标签             |
| -------------- | ------------ | ------------------- |
| `.thead-dark`  | `<th>` 黑色背景  | `<thead> & <tbody>` |
| `.thead-light` | `<th>` 浅灰色背景 | `<thead> & <tbody>` |

```css
/* bootstrap源码 */
.table .thead-dark th {
  color: #fff;
  background-color: #212529;
  border-color: #32383e;
}

.table .thead-light th {
  color: #495057;
  background-color: #e9ecef;
  border-color: #dee2e6;
}
```

> 案例1：所有的th都加上背景色

```html
<section class="container">
    <table class="table table-hover ">
      <thead class="thead-dark">
        <tr>
          <th>#</th>
          <th>First</th>
          <th>Last</th>
        </tr>
      </thead>
      <tbody class="thead-light">
        <tr>
          <th class="">1</th>
          <td>Mark</td>
          <td>Otto</td>
        </tr>
        <tr>
          <th>2</th>
          <td>Jacob</td>
          <td>Thornton</td>
        </tr>
      </tbody>
    </table>
</section>
```

### 仅针对 `<tbody> 下 <tr>`

> 仅应用于 `<tbody>` 下 `<tr>` 的背景样式

| css类             | 效果                      | 定义样式的标签   |
| ---------------- | ----------------------- | --------- |
| `.table-striped` | `<tbody>`条纹效果           | `<table>` |
| `.table-hover`   | `<tbody>`下改变当前行`<tr>`背景 | `<table>` |

```css
/* bootstrap源码 */
.table-striped tbody tr:nth-of-type(odd) {
  background-color: rgba(0, 0, 0, 0.05);
}

.table-hover tbody tr:hover {
  background-color: rgba(0, 0, 0, 0.075);
}
```

> 案例1： `<tbody>` 条纹效果，并改变当前行背景

```html
<section class="container">
    <table class="table-striped table-hover">
      <thead>
        <tr>
          <th>#</th>
          <th>First</th>
          <th>Last</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th class="">1</th>
          <td>Mark</td>
          <td>Otto</td>
        </tr>
        <tr>
          <th>2</th>
          <td>Jacob</td>
          <td>Thornton</td>
        </tr>
        <tr>
          <th class="">3</th>
          <td>Mark</td>
          <td>Otto</td>
        </tr>
      </tbody>
    </table>
</section>
```

### 应用于所有表格标签

> 以下样式，可应用于表格内所有标签，修改表格背景颜色

| css类               | 语义  |
| ------------------ | --- |
| `.table-primary`   | 基本  |
| `.table-secondary` | 第二  |
| `.table-success`   | 成功  |
| `.table-info`      | 信息  |
| `.table-warning`   | 警告  |
| `.table-danger`    | 危险  |
| `.table-light`     | 亮色  |
| `.table-dark`      | 暗色  |
| `.table-active`    | 活动  |

```css
/* bootstrap源码 */
.table-primary,
.table-primary > th,
.table-primary > td {
  background-color: #b8daff;
}

.table-hover .table-primary:hover {
  background-color: #9fcdff;
}

.table-hover .table-primary:hover > td,
.table-hover .table-primary:hover > th {
  background-color: #9fcdff;
}
```

> 案例：改变 `<tbody>`下所有行的最后一个<td>背景色

```html
<section class="container">
    <table class="table table-hover">
      <thead class="thead-dark">
        <tr>
          <th>#</th>
          <th>类</th>
          <th>语义</th>
        </tr>
      </thead>
      <tbody class="thead-light">
        <tr>
          <th>1</th>
          <td>.table-primary</td>
          <td class="table-primary">基本</td>
        </tr>
        <tr>
          <th>2</th>
          <td>.table-secondary</td>
          <td class="table-secondary">第二</td>
        </tr>
        <tr>
          <th>3</th>
          <td>.table-success</td>
          <td class="table-success">成功</td>
        </tr>
        <tr>
          <th>4</th>
          <td>.table-info</td>
          <td class="table-info">信息</td>
        </tr>
        <tr>
          <th>5</th>
          <td>.table-warning</td>
          <td class="table-warning">警告</td>
        </tr>
        <tr>
          <th>6</th>
          <td>.table-danger</td>
          <td class="table-danger">危险</td>
        </tr>
        <tr>
          <th>7</th>
          <td>.table-light</td>
          <td class="table-light">亮色</td>
        </tr>
        <tr>
          <th>8</th>
          <td>.table-dark</td>
          <td class="table-dark">暗色</td>
        </tr>
        <tr>
          <th>9</th>
          <td>.table-active</td>
          <td class="table-active">活动</td>
        </tr>
      </tbody>
    </table>
</section>
```
